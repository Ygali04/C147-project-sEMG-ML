# @package _global_
defaults:
  - _self_
#hydra:
#  launcher:
#    submitit_folder: ${hydra.run.dir}/submitit_logs/%j
#    timeout_min: 2880
#    nodes: ${trainer.num_nodes}
#    gpus_per_node: ${trainer.devices}
#    tasks_per_node: 1
